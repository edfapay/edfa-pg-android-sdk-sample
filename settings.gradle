pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        jcenter()
        mavenCentral()
    }
}
rootProject.name = "ExpressPayCardSample"
include ':app'
//include ':expresspay-sdk'
//project(':expresspay-sdk').projectDir = new File(settingsDir, '../expresspay-sdk')

